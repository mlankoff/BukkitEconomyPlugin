package com.minelogic.economy.misc;

import java.util.HashMap;
import java.util.UUID;

public class HashMaps {
	
	//HashMap contains specific player money
	public static HashMap<UUID, Integer> money = new HashMap<UUID, Integer>();
		
	//HashMap contains specific player tokens
	public static HashMap<UUID, Integer> tokens = new HashMap<UUID, Integer>();

}
